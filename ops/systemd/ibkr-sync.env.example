# Copy to /etc/lumisignals/ibkr-sync.env on prod and fill in the real value.
# chown root:root ibkr-sync.env && chmod 600 ibkr-sync.env

IBKR_SYNC_KEY=ibkr_sync_2026

# Optional alert email password (used by lumisignals.alerts).
ALERT_EMAIL_PASSWORD=
